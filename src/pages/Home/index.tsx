import React from 'react';
import { Button } from '@mui/material';

import { Container } from './styles';

const Home = () => (
  <Container>
    <Button>dtest</Button>
  </Container>
);

export default Home;
